<?php

declare(strict_types=1);

/**
 * Database migration class
 *
 * Neutralized — this was a broken copy-paste from another project (ALTERs
 * user with non-existent columns, Domibus junk). The real schema lives in
 * 20260916_000100_init.php. Kept as no-op so the migration runner still
 * records it.
 */

use \Skeleton\Database\Database;

class Migration_20251017_170550_Init extends \Skeleton\Database\Migration {

	/**
	 * Migrate up
	 *
	 * @access public
	 */
	public function up(): void {
	}

	/**
	 * Migrate down
	 *
	 * @access public
	 */
	public function down(): void {
	}
}
