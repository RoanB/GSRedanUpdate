<?php

/**
 * File
 *
 * Project-level file model. `lib/base/Bootstrap.php` sets
 * `\Skeleton\File\Config::$file_interface = 'File'`, so this is the class
 * skeleton-file instantiates for uploads and lookups. It adds no behaviour of
 * its own; keeping it here lets the project override file behaviour later
 * without touching the package.
 *
 * @author Roan Buysse <roan@tigron.be>
 */

class File extends \Skeleton\File\File {
}
